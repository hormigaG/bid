import { LeftPadFilterPipe } from './left-pad-filter.pipe';

describe('LeftPadFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftPadFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
